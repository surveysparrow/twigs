import { Button, Flex, Table, Thead, Th, Tbody, Tr, Td, Input, FormLabel } from "@sparrowengg/twigs-react";
import { LiveProvider, LiveEditor, LiveError, LivePreview } from "react-live";
import { Divider } from 'docs/src/components/common'

# FormLabel

FormLabel can be used for adding label for your input fields. Twigs also comes with a `FormInput` component, which wraps Label and Input in a single component. 

### Import

```jsx
import { FormLabel } from "@sparrowengg/twigs-react";
```
<Divider />

### Usage 
```jsx live
<FormLabel> Email Address </FormLabel>
```
<Divider />

### Props
| Property       | Description                                | Type                    | Default |
|----------------|--------------------------------------------|-------------------------|---------|
| size           | Size of the FormLabel                       | xs,sm                   | sm      |
