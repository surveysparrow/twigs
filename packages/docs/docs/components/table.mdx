import { PlaygroundLink } from "@site/src/components/common/playground-link";
import TableSnippet from "@site/static/doc-snippets/table.mdx";

# Table

### Import

```jsx
import { Table, Tbody, Td, Th, Thead, Tr } from "@sparrowengg/twigs-react";
```

All these components are a wrapper around the html elements `table`, `tbody`, `td`, `th`, `thead`, `tr` respectively.

### Usage

<PlaygroundLink name="table" />
<TableSnippet />

### Props

| Property | Description | Type | Default |
|-----------|-------------|------|----------|
| border | Used to determine how borders are added within the table | `horizontal` \| `vertical` \| `both` | `horizontal` |
| size | Controls the spacing and padding within table cells | `medium` \| `small` | `medium` |
| sticky | Keeps the table header fixed while scrolling | `boolean` | `false` |
| hover | Highlights table rows on mouse hover | `boolean` | `true` |

