import { PlaygroundLink } from "@site/src/components/common/playground-link";
import TableSnippet from "@site/static/doc-snippets/table.mdx";

# Table

### Import

```jsx
import { Table, Tbody, Td, Th, Thead, Tr } from "@sparrowengg/twigs-react";
```

All these components are a wrapper around the html elements `table`, `tbody`, `td`, `th`, `thead`, `tr` respectively.

### Usage 

<PlaygroundLink name="table" />
<TableSnippet />