{/* theme-provider.stories.mdx */}

import { Meta, Story } from '@storybook/addon-docs';
import { Box } from '../../box/box';
import { defaultTheme } from '../../../stitches.config';
import { Text } from '../../text/text';
import { Heading } from '../../heading/heading';
import { Flex } from '../../flex/flex';
import { Button } from '../../button';
import {
  AllColors,
  AllFontSizes,
  AllFontWeights,
  AllBorderWidths,
  AllRadii,
  AllSpaces,
  AllSizes
} from './theme-blocks';

export const themeData = defaultTheme;
export const themeString = JSON.stringify(defaultTheme, undefined, 2);

<Meta
  title="Theme"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

## Installation

```
yarn install @sparrowengg/twigs-react @sparrowengg/twigs-react-icons
```

Twigs come with a `ThemeProvider` which is completely customizable. With the help of `ThemeProvider`, you can modify attributes such as colors, font sizes, spacing, etc.

<ol>
  <li>
    <a href="#colors">Colors</a>
  </li>
  <li>
    <a href="#fontSize">Font Size</a>
  </li>
  <li>
    <a href="#fontWeights">Font Weights</a>
  </li>
  <li>
    <a href="#borderWidths">Border Widths</a>
  </li>
  <li>
    <a href="#borderRadius">Border Radius</a>
  </li>
  <li>
    <a href="#spaces">Spaces</a>
  </li>
  <li>
    <a href="#sizes">Size</a>
  </li>
  <li>
    <a href="#using-custom-css">How to do custom CSS</a>
  </li>
</ol>

Basic usage of `ThemeProvider`

```jsx
import { ThemeProvider } from '@sparrowengg/twigs-react';

const Index = () => (
  <ThemeProvider>
    <App /> //your app entry point
  </ThemeProvider>
);
```

Optionally you can pass a `theme` object to `ThemeProvider` to modify the default theme.

```jsx
<ThemeProvider
  theme={{
    fonts: {
      body: "'DM sans', sans-serif",
    },
  }}
>
  <App /> //your app entry point
</ThemeProvider>
```

Below is the complete theme object which you can modify further.

<Box css={{ maxHeight: '500px', overflowY: 'scroll' }}>
  <pre>{themeString}</pre>
</Box>

<Story name="Default">
  <Flex gap='100px' flexDirection='column' css={{ margin: '100px 0px' }}>
    <AllColors />
    <AllFontSizes />
    <AllFontWeights />
    <AllBorderWidths />
    <AllRadii />
    <AllSpaces />
    <AllSizes />
  </Flex>
</Story>



<Story name="CustomCSS">
<Heading size='h5' id='using-custom-css'>Using custom CSS </Heading>

If you'd like to modify css of any Twigs component, you can `css` prop to give custom CSS.

<Button 
  size='lg'
  css={{
    background: '$accent700',
    fontWeight: '$4',
    marginTop: 12
  }}
>
  Save
</Button>
</Story>

```
<Button 
  size='lg'
  css={{
    background: '$accent700',
    fontWeight: '$4'
  }}
>
  Save
</Button>
```