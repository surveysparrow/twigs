---
title: Accordion
description: A vertically stacked set of interactive headings that each reveal a section of content. Perfect for organizing content in collapsible sections with smooth animations and accessibility features.
---

## Import

```jsx
import {Accordion,AccordionContent,AccordionItem,AccordionTrigger} from "@sparrowengg/twigs-react";
```

## Usage

```jsx
const AccordionExample = () => {
  return (
    <Accordion type="single">
      <AccordionItem value="item-1">
        <AccordionTrigger>Page settings</AccordionTrigger>
        <AccordionContent>
          Configure your page settings including layout, theme, and display options.
        </AccordionContent>
      </AccordionItem>

      <AccordionItem value="item-2">
        <AccordionTrigger>Profile Information</AccordionTrigger>
        <AccordionContent>
          Manage your profile details, avatar, and personal preferences.
        </AccordionContent>
      </AccordionItem>
    </Accordion>
  );
};
```

<Playground snippetName="accordion" defaultPlayground />

## Props

<TypeTable
  type={{
    type: {
      type: "'single' | 'multiple'",
      description: "Controls whether one or multiple items can be opened at the same time.",
      default: "'single'",
    },
    size: {
      type: "'sm' | 'md'",
      description: "Size variant of the accordion.",
      default: "'sm'",
    },
    defaultValue: {
      type: "string | string[]",
      description: "The default value of the item to expand when uncontrolled.",
    },
    value: {
      type: "string | string[]", 
      description: "The controlled value of the item to expand.",
    },
    onValueChange: {
      type: "(value: string | string[]) => void",
      description: "Event handler called when the expanded state changes.",
    },
    collapsible: {
      type: "boolean",
      description: "When type is single, allows closing content when clicking trigger for opened item.",
      default: "false",
    },
    disabled: {
      type: "boolean",
      description: "When true, prevents the user from interacting with the accordion.",
      default: "false",
    },
  }}
/>

### AccordionItem

<TypeTable
  type={{
    value: {
      type: "string",
      description: "A unique value for the item.",
    },
    disabled: {
      type: "boolean", 
      description: "When true, prevents the user from interacting with the item.",
      default: "false",
    },
  }}
/>

### AccordionTrigger

<TypeTable
  type={{
    children: {
      type: "ReactNode",
      description: "The content of the trigger.",
    },
  }}
/>

### AccordionContent

<TypeTable
  type={{
    children: {
      type: "ReactNode", 
      description: "The content to show when the item is expanded.",
    },
  }}
/>

## Examples

### Multiple Selection

Allow multiple accordion items to be open simultaneously by setting `type="multiple"`.

<Playground snippetName="accordion-multiple" />

### With Default Value

Set a default expanded item using `defaultValue`.

<Playground snippetName="accordion-default-value" />

### Disabled Items

Individual accordion items can be disabled to prevent user interaction.

<Playground snippetName="accordion-disabled" />

### Collapsible Single

When `type="single"`, you can allow the user to close the currently open item by setting `collapsible={true}`.

<Playground snippetName="accordion-collapsible" />
