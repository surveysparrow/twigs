---
title: Text
description: A versatile text component for displaying content with consistent typography. Perfect for headings, body text, captions, and labels with customizable sizes, weights, and truncation options.
---

## Import

```jsx
import { Text } from "@sparrowengg/twigs-react";
```

## Usage

```jsx
const TextExample = () => {
  return (
    <Box css={{ display: "flex", flexDirection: "column", gap: "$4" }}>
      <Text size="lg" weight="bold">Large Bold Heading</Text>
      <Text size="md" weight="medium">Medium Weight Subheading</Text>
      <Text size="sm" weight="regular">Regular body text content</Text>
      <Text size="xs" weight="regular" truncate>
        This is a long text that will be truncated with ellipsis
      </Text>
    </Box>
  );
};
```

<Playground snippetName="text-basic" defaultPlayground />

## Props

### Text

<TypeTable
  type={{
    size: {
      type: "'xxs' | 'xs' | 'sm' | 'md' | 'lg'",
      description: "The font size of the text component.",
      default: "'sm'",
    },
    weight: {
      type: "'regular' | 'medium' | 'bold'",
      description: "The font weight of the text component.",
      default: "'regular'",
    },
    truncate: {
      type: "boolean",
      description: "When true, truncates text at the first line and adds trailing ellipsis.",
      default: "false",
    },
    showLines: {
      type: "number",
      description: "Truncates text after the specified number of lines with ellipsis.",
    },
    children: {
      type: "ReactNode",
      description: "The text content to be displayed.",
    },
    css: {
      type: "object",
      description: "Custom CSS styles for the component.",
    },
  }}
/> 